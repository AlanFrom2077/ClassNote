本节课讲了OOP oriented object programming 面向对象编程的基础知识

1. OOP相关概念
    核心概念：
        对象 object
        类 class 
        引用 reference

    三者之间的关系：
        类 定义了 对象 的属性和行为， 引用用于存储对象，对象可以通过dreference操作找到
        Java中的一切都是通过object和class进行构建的
        
        note: 
            但是有例外，8中基本数据类型boolean byte short int long float double char 不是

        下面展示了一个class example：
            class student {
                public String name;              ->   这些variable都被叫做field字段
                public int age;
                public int id;

                public student(String name, int age, int id){ -> 特殊的method，construct method构造方法
                    this.name = name;           -> this关键字代指实例化的对象 . 就是dereference操作
                    this.age = age;
                    this.id = id;

                public void study(){            -> 一个普通的方法 public是access identifer, void是return value, ()里可以填参数
                        System.out.println("I'm studying")
                    }
                }
            }              instantiation实例化
                                  |
            Student newStudent = new Student("Renzhe Xu", 18, 21238401);
                       |                          |
                 declaration声明            initialization初始化

2. 栈和堆
    栈和堆是计算机中的两种类型的内存
    栈：
        用于存储：
            1. 函数相互调用的顺序(栈帧)
            2. local variable
                    local variable有两个情况
                    -> 栈存 name + value case：primitive type
                    -> 栈存 name + address => reference case: 除了基础类型以外的所有类型

    堆：
        用于存储：
            1. Method Area用于存储代码定义
            2. non-local varialbe -> like: class fields

3. default value
    Java中有两种类型的variable
        1. local variable -> method中的variable
        2. fields -> out of the method
    
    default value:
        基于variable类型的不同，会有不同的default value：
            1. primitive type vs. object 
                -> primitive type     default value: 零假空原则
                -> object             default value: null

            2. local variable vs fields
                -> local variable cannot have default value
                -> fields can have default value

4. naming convention:
    class name -> UpperCamelCase
    method name, vairalbe name -> lowerCamelCase
    constant: UPPER_SNAKE_CASE

    
            
    
            
