OOP基础 2

这节课将会更深入的讲解OOP相关的细节：
outline: 1. static
         2. final
         3. parameter passing 
         4. array

这节课我们使用下面这个class作为例子：

public class Student{
    public int age;
    private final String name;
    private static String school;
    private static final GREETINGS = "hello";

    public Student(String name){                     这是一个constructor，构造方法，当没有显性地声明的时候，class会隐
        this.name = name;                         -> 性地声明一个没有任何内容的构造方法 default constructor
    }                                            

    puclic int getAge(){                          
        return age; 
    }                                             ->
                                                     这里有三个类成员函数 getAge(), setAge(), getName(), public和
    public void setAge(int age){                  -> private在这里结合使用。可以发现，在class中的fields声明为了private，
        this.age = age;                              所以在程序外，我们无法对这些声明为private fields进行直接access，
    }                                             -> 我们只能使用后面的三个public的method进行访问，这种方式会使你的程序
                                                     更加的安全可用
    public String getName(){                       
        return name;
    }
}

重点1：关键字static
    static可以修饰的东西：variable, function, class
    static表示的意义: 从属于class的东西
    static的调用方式：利用class name进行调用

    如何决定static是否要加上？
        -> 完全取决于static对象自己的实际物理意义，即所有的对象是否具有共同的属性/特性/行为
           如果有，我们要对这些 属性/特性/行为 用static进行修饰

    static的使用范围:
        如果在类中，我们有一个static方法，这个static方法只能使用static的variable或者其他东西
            -> 规则可以总结为: 公家的只能用公家的，个人的可以用公家的或者个人的
            -> 为什么？
                recall 因为static是属于类本身的东西，所以我们可以在不对类instantiation的前提
                下，利用class name 进行访问和使用，但是此时因为对象还没有被声明，因此如果使用了
                obj的定义，就会导致在调用时报错

重点2：final关键字
    final关键字的使用对象：variable/class/method
    本节课只说final在variable上的应用
    
    使用场景：
        当有一个variable的值不想要被改变时，我们就可以利用final关键字对variable进行修饰，将
        variable声明为常量

重点3：parameter passing    
    在java中，parameter总是
