在基础班主要提及了以下几种sorting算法

1. selection sort
selection sort的逻辑很简单，就是不断地取最小值放在一边，或者说进行交换
这样说可能有点难理解，可以参考下面的代码:

public int[] selectionSort(int[] target){
    for(int i = 0; i <= target.length - 1; i++){
        for(int j = 0; j <= target.length - 1; j++){
            if(target[j] > target[i]){
                temp = target[i];
                target[i] = target[j];
                target[j] = temp
            }
        }
    }
    return target;
}
对于selection sort来说，时间复杂度是O(n^2), 空间复杂度是O(1)

2. merge sort
merge sort的主要思想就是通过不断地进行二分，将一个完整array不断地进行拆分，直到拆成一个个单独的元素，因为单个元素
天然有序，所以不用进行比较，之后再将排好的对象进行比较，组合，就会得到一个组合好的有序的array

class MergeSort{
    int[] result;
    public MergeSort(int[] target){
        int left = 0;
        int right = target.length - 1;
        this.result = mergeSort(target, left, right);
    }

    public int[] mergeSort(int[] target, int left, int right){
        if(left == right){
            return new int[]{target[left]};
        }
        int mid = left + (right - left)/2;
        int[] leftSubArray = mergeSort(target, left, mid);
        int[] rightSubArray = mergeSort(target, mid+1, right);

        return merge(leftSubArray, rightSubArray);
    }

    public int[] merge(int[] leftSubArray, int[] rightSubArray){
        int i = 0;
        int j = 0;
        int[] temp = new int[leftSubArray.length + rightSubArray.length];

        for(int k = 0; k < temp.length; k++){
            if(i >= leftSubArray.length){
                temp[k] = rightSubArray[j];
                j++;
            }
            else if(j >= rightSubArray.length){
                temp[k] = leftSubArray[i];
                i++;
            }
            else{
                if(leftSubArray[i] <= rightSubArray[j]){
                    temp[k] = leftSubArray[i];
                    i++;
                }
                else{
                    temp[k] = rightSubArray[j];
                    j++;
                }
            }
        }
        return temp;
    }
}

对于mergesort的时间复杂度分析，mergesort的时间复杂度为O(n log n)
空间复杂度则为recursion tree的最大深度O(log n)

3. quick sort
quick sort的思路和merge sort有相似之处，都是利用分治法和recursion不断地缩小sorting的范围，
直到最后只剩下一个数字，天然有序

quick sort的具体思路是，在乱序数组中随机选取一个数字，以这个数字为基准，将剩下的元素分成比基准大的数字的数组(大数组)
和比基准小的数字的数组(小数组)，将他们以 小数组，基准，大数组 的顺序排列，再对小数组和大数组重复上述操作，直到只剩下一个元素
天然有序。代码如下：

class QuickSort{
    public QuickSort(int[] target){
        int left = 0;
        int right = target.length-1;
        quickSort(target, left, right);
    }

    private void quickSort(int[] target, int left, int right){
        if(left >= right){ //不能改成==或者>, ==保护的是array只有一个元素的情况，>保护的是空区间的情况，空区间是由下面的while决定的，没有办法避免
            return;
        }
        int ref = target[right];
        int i = left;
    int j = right-1;
    while(i <= j){
        if(target[i] < target[right]){
            i++;
        }
        else{
            swap(target, i, j);
            j--;
        }
    }

    swap(target, i, right);

    quickSort(target, left, i-1);
    quickSort(target, i+1, right);
    }
    
    private void swap(int[] target, int i, int j){
        int temp = target[j];
        target[j] = target[i];
        target[i] = temp;
    }
}


