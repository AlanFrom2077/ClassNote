这节课主要讲解了关于Java的基础语法，包括Java的8种基本类型，二进制相关，数据类型的显式和隐式转化，以及
一些常用的运算符，包括赋值，算数，逻辑等等，具体笔记如下：

1. 原码(true form) 反码(one's complement) 补码(two's complement)

    十进制转二进制原码：
    10 -> 01010 //最高位为符号位 0为正 1为负

    二进制原码转反码：
    每一位取反得到 10101 表示10的反码，即为-10 //转反码的方式就是每一位取反
    但是会出现1000和0000的正负零的情况，所以为了方便计算我们会引入补码

    反码转补码：
    反码转补码时，直接末位+1即可，例如0101为5，1011为-5

    反码(one's complement) can only represent -(2^N-1 - 1) to +(2^N-1 -1) using N bits
    补码(two's complement) can represent -(2^N-1) to +(2^N-1 - 1) using N bits

    所以当给定了一个二进制源码的时候，如果要让我们转成他的复数，我们就要进行 反码 然后末位+1，取对应的补码，
    这样才能得到我们想要的答案

    // 解释：这里N-1次方是因为减掉了首项符号位，-1是因为要减掉0，反码两边都要减0是因为对于反码来说同时存在正负两个0，因此要减去

2. 基本数据类型
    byte -> 8-bit integer = 1 byte
    short -> 16-bit integer
    int -> 32-bit integer
    long -> 64-bit integer
    float -> 32-bit floating
    double -> 64-bit floating
    char -> unicode character 2 byte
    boolean -> boolean variable 1 byte

3.默认值
    在定义变量时，如果不显示定义值的话，java就会赋予默认值，默认值基本为零假空组成 //注意这里说的变量是成员变量，对于局部变量jvm不会赋予其默认值
    零(0) -> 除boolean外的基本类型
    假(false) -> 特指boolean的默认值
    空(null) -> 一切引用类型的默认值都是null //数组是个特例，对于数组而言虽然数组

4.运算符
    运算符: + - * ** / % //注意，/的结果是向下取整，计算的结果会根据计算双方的数据类型自动判断是否保留小数部分
    比较运算符: == > < >= <= !=
    赋值运算符：= += -= *= /= %=
    自增/自减运算符：++ -- //注意i++是执行完代码后加，++i是执行代码前加
    逻辑运算符: && || !

5.类型转换
    隐式转换implicit conversion：当一个较小的数据类型的值赋值给一个较大的数据类型会发生隐式转换，同时在进行不同数据类型的运算时也会自动提升小单位的类型
    显示转换explicit conversion：大转小时需要手动进行转换，称为显示转换

    widening: 小char -> int -> long -> float -> double大