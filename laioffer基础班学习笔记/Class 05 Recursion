Recursion递归

1. recursion的定义以及编写
递归其实很好理解，就是不断地调用自己本身以达成目的的一种方式，在高级编程语言中基本上都能使用这种方式
在定义递归的时候有两个东西是比较重要的，有了这两个东西才能防止我们在使用递归的时候陷入无限递归：
    1. input size减少 -> 在使用递归的时候一定要保证我们的input size是在不断减小最终向着base case前进的
    2. base case -> base case其实就是给recursion定义了一个停止的条件

在这两者的共同作用下，我们在使用递归的时候才不会陷入到无限递归中
根据这两个必要条件，我们可以总结出定义recursion的时候所需要经历的步骤, 一共有3+1步：
    Step 1. Define Base Case: 定义结束条件
    Step 2. Define subproblem: 定义子问题，即比原问题小一号的问题
    Step 3. Define Recursion Rule: 定义如何从原问题变为子问题进而指向base case的规则

    Extra Setp(面试用): function在做什么？不要用问题去描述问题，利用我们定义recursion的时候所知的3个step
                        对recursion function进行描述

2. 时空复杂度分析
在比较算法的快慢的时候，我们经常会用到一个指标，时空复杂度，来衡量算法的效率
使用时空复杂度来分析的意义在与为了减少电脑性能差异所带来的变量


3. recursion的时间空间复杂度分析
先看时间复杂度：
这里先给出定义，recursion的时间复杂度为recursion tree中的每一个节点的时间的总和

这里我们引出recurrence relation递归式帮助我们来理解和描述递归算法的运行时间，形式如下：
    T(n) = a . T(n/b) + f(n)

    T(n) 为求解规模为n的问题所需的时间
    a 为每次递归调用产生的子问题数目(分支数)
    n/b 每个子问题的规模(缩小到原来的1/b)
    f(n) 除递归调用外，本层的开销

因此，recursion的时间复杂度就会有以下两个公式帮助计算:
    when branch = 1: O(level * 每一个function的时间)
    when branch > 1: O(branch ^ level * 每一个function的时间)
    当然，使用这两个计算方式的条件是recursion tree规则且每一个function运行时间均等的情况下
    不过不用担心，一般情况下面试都会使用规则的来考，不规则的确实有点太为难人了，把一个算法题变成了一个数学问题

这里给出一个概念：主定理 //后期有时间了得来看看是怎么个事

接下来来看空间复杂度：
这里先给出定义，recursion的空间复杂度就是 递归最大深度(递归树最大高度)x每层栈帧的大小

这个比较好理解，因为每次调用方法会在栈内开额外的栈帧，就会消耗空间
在递归的过程中，会不断开新的栈帧，为了给增长率定一个上界，就可以直接定最大深度x每层栈帧的大小作为上界，因为不可能有更差的情况了

需要注意，这里讨论的还是栈帧大小均等的情况下


