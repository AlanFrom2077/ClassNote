/*给定一个仅包含整数的二维矩阵，每行按升序排列。下一行的第一个元素大于（或等于）上一行的最后一个元素。

给定一个目标数字，返回该目标数字在矩阵中的位置。如果目标数字在矩阵中不存在，则返回 {-1, -1}。

比喻：
    给定的矩阵不为空，且大小为N * M，其中N >= 0 且M >= 0。
例子：
    matrix = { {1, 2, 3}, {4, 5, 7}, {8, 9, 10}}
    target = 7，返回 {1, 2}
    target = 6，返回 {-1，-1} 表示目标数字在矩阵中不存在。
*/

public int[] matrixBinarySearch(int[][] matrix, int target){
    if(matrix == null || matrix.length == 0){
        return new int[]{-1,-1};
    }
    int row = matrix.length;
    int col = matrix[0].length;

    int range = row*col - 1;
    int left = 0;
    int right = range - 1;
    
    while(left <= right){
        int mid = left + (right - left)/2;
        int midRow = mid / row;
        int midCol = mid % row;

        if(matrix[midRow][midCol] == target){
            return new int[]{midRow, midCol};
        }
        else if(matrix[midRow][midCol] > target){
            right = mid - 1;
        }
        else if(matrix[midRow][midCol] < target){
            left = mid + 1;
        }
    }
    return new int[]{-1, -1}；
}

/*
思路基于BS母题1：

其实很简单，这道题已经给定条件，sorted的matrix，search target，那就得反应到BS
但是问题在于，matrix是二维数据，无法直接使用BS，如何把一个二维matrix转化为一个一维array使用BS是问题关键
    引入公式：
        indexRow = index / col
        indexCol = index % col 
通过这两个公式，我们可以把一个1D的index转为一个2D的matrix坐标，这样就把matrix转化为了一个array
接下来通过BS求解即可
*/